import { useQuery } from "@tanstack/react-query";
import { getAdminAnalytics } from "../../function/getAdminAnalytics";

export const useAdminAnalytics = () => {
    return useQuery({
        queryKey: ["admin-analytics"],
        queryFn: () => getAdminAnalytics(),
    });
};
